import React from 'react';
import styled from 'styled-components';

const StyledUpdatingWrapper = styled.div`
  background-color: red;
`;

const Updating = () => {
  return <StyledUpdatingWrapper>업데이트중...</StyledUpdatingWrapper>;
};

export default Updating;
